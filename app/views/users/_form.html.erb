<% content_for :head do %><%= stylesheet_link_tag 'login_signup', media: 'all', 'data-turbolinks-track': 'reload' %><% end %>

<div class="center">
  <%= content_for :form_title %>
  <%= form_for(@user) do |f| %>
    <div class="form_field">
      <%= f.text_field :username, required: true %>
      <%= content_tag(:span) %>
      <%= f.label :username %>
    </div>
    <div class="form_field">
      <%= f.text_field :email, required: true %>
      <%= content_tag(:span) %>
      <%= f.label :email %>
    </div>
    <div class="form_field">
      <%= f.file_field :cover, required: true %>
      <%= content_tag(:span) %>
    </div>
    <div class="form_field">
      <%= f.password_field :password, required: true %>
      <%= content_tag(:span) %>
      <%= f.label :password%>
    </div>
    <div class="form_field">
      <%= f.password_field :password_confirmation, required: true %>
      <%= content_tag(:span) %>
      <%= f.label :password_confirmation %>
    </div>
  
    <div class="submit">
      <%= f.submit %>
    </div>
  <% end %>

  <% if !logged_in? %>
  <div class="alternative_link">
    Already a member? <%= link_to "Sign in", login_path %>
  </div>

  <div class="oauth_link">
        or you can sign in with <%= link_to 'auth/google_oauth2', method: 'POST' do %><%= '<i class="fa-solid fa-g"></i>oogle'.html_safe %><% end %> 
  </div>
  <% end %>
</div>
